[build]
  base = "/"
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist/vite"

[build.environment]
  NODE_VERSION = "16.20.2"
  NPM_VERSION = "8.19.4"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
